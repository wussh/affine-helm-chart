# One-time Helm-to-ArgoCD ownership adoption. No credentials.
# secrets.mode=existing: the live affine-database/affine-redis Secrets are kept as-is.
application:
  enabled: true
migration:
  enabled: false
prerequisites:
  enabled: false
secrets:
  mode: existing
routing:
  mode: none
