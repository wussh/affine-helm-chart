# Static test fixture: everest prerequisites in an external namespace with
# externally managed Secrets (secrets.mode=existing). No credentials.
application:
  enabled: true
migration:
  enabled: true
prerequisites:
  enabled: true
  database:
    mode: everest
    name: db-affine
    namespace: prod
    userSecretName: everest-secrets-db-affine
  redis:
    enabled: true
secrets:
  mode: existing
  database:
    name: affine-database
  redis:
    name: affine-redis
routing:
  mode: none
